import type { Metadata } from "next"

export const metadata: Metadata = { title: "Nouveau litige" }

export default function NewDisputeLayout({ children }: { children: React.ReactNode }) {
  return children
}
