import type { Metadata } from "next"

export const metadata: Metadata = { title: "Nouvelle demande" }

export default function NewMissionLayout({ children }: { children: React.ReactNode }) {
  return children
}
