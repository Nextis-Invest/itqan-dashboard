import type { Metadata } from "next"

export const metadata: Metadata = { title: "Nouveau service" }

export default function NewGigLayout({ children }: { children: React.ReactNode }) {
  return children
}
